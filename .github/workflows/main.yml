name: GPU Workflow
on: 
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: nvidia/cuda:11.0.3-base-${ubuntu20.04}
    steps:
      - name: Check GPU
        run: nvidia-smi
