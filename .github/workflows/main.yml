name: GPU Workflow
on: 
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: nvidia/cuda:11.0-base
    steps:
      - name: Check GPU
        run: nvidia-smi
